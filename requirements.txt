# Core pipeline
pydantic>=2.0
pdfplumber>=0.11
pikepdf>=9.0  # repairs PDFs with a broken xref/trailer that pdfplumber can't even open
openpyxl>=3.1
python-docx>=1.1
httpx>=0.28
pymongo>=4.6  # backend/db.py -- writes pipeline results into the same Atlas cluster the frontend reads

# Existing API experiments (GmailAPI.py, OCRApi.py)
google-auth-oauthlib
google-api-python-client
google-cloud-documentai
python-dotenv

# Dev
pytest>=8
pytest-asyncio>=0.23
groq>=1.0  # fallback provider if the AI Gateway is rate-limited/unavailable
